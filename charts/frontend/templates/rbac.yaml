# apiVersion: rbac.authorization.k8s.io/v1
# kind: Role
# metadata:
#   name: {{ include "frontend.fullname" . }}-role
#   namespace: {{ .Release.Namespace }}
# rules:
#   # Example: read-only access to ConfigMaps in its namespace (adjust to your needs)
#   - apiGroups: [""]
#     resources: ["configmaps"]
#     verbs: ["get","list","watch"]

# ---
# apiVersion: rbac.authorization.k8s.io/v1
# kind: RoleBinding
# metadata:
#   name: {{ include "frontend.fullname" . }}-rb
#   namespace: {{ .Release.Namespace }}
# subjects:
#   - kind: ServiceAccount
#     name: {{ include "frontend.serviceAccountName" . }}
#     namespace: {{ .Release.Namespace }}
# roleRef:
#   kind: Role
#   name: {{ include "frontend.fullname" . }}-role
#   apiGroup: rbac.authorization.k8s.io
